CREATE TABLE users (id BIGINT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(255) NOT NULL,email VARCHAR(255) NOT NULL UNIQUE,password VARCHAR(255) NOT NULL,bairro VARCHAR(255),cidade VARCHAR(255),created_at DATETIME NOT NULL,updated_at DATETIME NOT NULL);
