# Opcional para Docker local. Substitua senhas e chave antes de publicar.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=inova
DB_USERNAME=inova
DB_PASSWORD=inova_pass
MYSQL_ROOT_PASSWORD=inova_root_pass

JWT_SECRET=inova-local-development-only-secret-change-before-deploy
JWT_EXPIRATION=86400000

SERVER_PORT=8080
FRONTEND_PORT=8088
MYSQL_HOST_PORT=3307
