# Optional ports for IDE/database clients. The standard stack exposes only the site.
services:
  mysql:
    ports:
      - "127.0.0.1:${MYSQL_HOST_PORT:-3307}:3306"
  inova-api:
    ports:
      - "127.0.0.1:${SERVER_PORT:-8080}:8080"
